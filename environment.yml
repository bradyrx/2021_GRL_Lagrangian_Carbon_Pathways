name: brady-carbonpathways 
channels:
  - conda-forge
dependencies:
  # IDE
  - jupyter
  - jupyterlab
  - jupyter-server-proxy
  # Input/Output
  - netcdf4
  - zarr
  # Numerics
  - esmtools
  - numpy
  - scipy
  - xarray
  - xhistogram
  # Performance
  - bottleneck
  - dask
  # Visualization
  # Fixes a bug with dask dashboard.
  - bokeh<2.0.0
  - cartopy
  - matplotlib
  - nc-time-axis
  - proplot
  - pip>=20.1
  - pip:
      - ncar_jobqueue
      - PyCO2SYS
      - gsw
      - nb_black 
